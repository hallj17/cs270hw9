<%# create form_for with first name, last name, email %>

<%= form_for @user, url: {action:
"create" }, do |f| %>
    <%= f.text_field :first_name, 'First Name' %>
    <%= f.text_field :last_name, 'Last Name' %>
    <%= f.text_field :email, 'Email' %>
    <%= f.submit "Edit" %>

<% end %>